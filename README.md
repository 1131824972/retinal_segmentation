# 视网膜血管分割

## 🚀 快速开始
### 启动服务
```angular2html
安装 Docker Desktop -> 在根目录运行 docker-compose up --build -> 访问 localhost:8000/docs
```

### 关闭服务
```angular2html
docker-compose down
```
